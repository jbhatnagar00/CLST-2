version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - # Your build commands here
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build  # or dist, depending on your framework
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
